{
  "name": "organise-this",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Jono Clarnette",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mrjono1/OrganiseThis.git"
  },
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "eslint 'src/**'",
    "test": "jest --config jest.config.js --silent",
    "coverage": "yarn test --coverage",
    "pub": "tsc && cp package.json /dist && cp README.md /dist && cd /dist && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "@types/node": "^18.0.0",
    "@typescript-eslint/eslint-plugin": "^5.30.0",
    "@typescript-eslint/parser": "^5.30.0",
    "eslint": "^8.18.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.1.0",
    "jest": "^28.1.1",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4",
    "yarn": "^1.22.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
