/**
 * Keep the best Calendars
 */
export interface SelectionSettings {
  /**
   * Defaults to 20
   */
  numberOfCalendars: number;
  /**
   * Defaults to `true`
   */
  enabled: boolean;
}
